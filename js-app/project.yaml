templates:
  - ../template/common.yaml
  - ../template/compose.yaml

kotlin:
  js:
    '':
      browser: { }
      nodejs: { }
      binaries:
        executable: true

  sourceSets:
    commonMain:
      dependencies:
        - ../kmp-lib
        - ../cmp-lib
        - ../shared
