templates:
  - ../template/common.yaml
  - ../template/compose.yaml

application: true

kotlin:
  js:
    '': { }

  sourceSets:
    commonMain:
      dependencies:
        - ../kmp-lib
        - ../cmp-lib
        - ../shared
