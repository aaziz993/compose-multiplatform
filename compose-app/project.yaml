type: APP

templates:
  - ../template/common.yaml
  - ../template/kmp.yaml
  - ../template/cmp.yaml

kotlin:
  sourceSets:
    - commonMain:
        dependencies:
          - :kmp-lib
          - :cmp-lib
          - :shared

android:
  buildFeatures:
    compose: true
