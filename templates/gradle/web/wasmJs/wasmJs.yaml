kotlin:
  targets:
    - wasmJs:
        compilerOptions:
          freeCompilerArgs:
            - -Xwasm-debugger-custom-formatters
        devServer:
          static: [ src@wasmJs ]

#  sourceSets:
#    - wasmJsMain:
#        dependencies:
#          - $kotlinWrappers.browser
