script:
  - kotlin:
      sourceSets:
        - named: |
            ("appleMain") { sourceSet ->
              sourceSet.dependencies {
                implementation(libs("ktor")["client.darwin"])
              }
            }
