imports:
  - wasm-js.yaml
script:
  - kotlin:
      targets:
        - withType<KotlinWasmJsTargetDsl>: |
            {
              outputModuleName.set(project.moduleName)
              browser {
                  commonWebpackConfig { commonWebpackConfig ->
                      commonWebpackConfig.outputFileName = "${project.moduleName}.js"
                  }
              }
              binaries.executable()
            }
