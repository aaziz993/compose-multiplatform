config:
  imports:
    - android-library-plugin.yaml
    - android-library.yaml
script:
  - kotlin:
      targets:
        - withType<KotlinAndroidTarget>: |
            {
              publishLibraryVariants(
                "release",
              )
            }