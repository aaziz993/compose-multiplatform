script:
  - plugins:
      - id: (libs.plugins("android.library").pluginId)
  - kotlin:
      targets:
        - withType<KotlinAndroidTarget>: |
            {
              publishLibraryVariants(
                "release",
              )
            }
