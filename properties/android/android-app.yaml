imports:
  - android-app-plugin.yaml
  - android.yaml
script:
  - android:
      buildTypes:
        - getByName: |
            ("release") {
              isShrinkResources = true
            }
        - getByName: |
            ("debug") {
              isShrinkResources = false
            }
