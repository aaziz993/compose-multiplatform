config:
  imports:
    - android-app-plugin.yaml
    - android-app.yaml
script:
  - android:
      buildTypes:
        - getByName: |
            ("release") {
              isShrinkResources = true
            }
        - getByName: |
            ("debug") {
              isShrinkResources = false
            }
