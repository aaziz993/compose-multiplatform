script:
  - android:
      buildTypes:
        - getByName: |
            ("release") {
              isShrinkResources = true
            }
        - getByName: |
            ("debug") {
              isShrinkResources = false
            }
