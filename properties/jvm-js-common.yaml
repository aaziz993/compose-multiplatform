script:
  - kotlin:
      sourceSets:
        - named: |
            ("jvmJsCommonMain") { sourceSet ->
              sourceSet.dependencies {
                implementation(libs["compass.geocoder.web.googlemaps"])
                implementation(libs["connectivity.http"])
              }
            }
