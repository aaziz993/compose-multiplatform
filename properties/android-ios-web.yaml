script:
  - kotlin:
      sourceSets:
        - named: |
            ("androidIosWebMain") { sourceSet ->
              sourceSet.dependencies {
                implementation(libs("webrtc.kmp"))
              }
            }