imports:
  - js.yaml
script:
  - kotlin:
      targets:
        - withType<KotlinJsTargetDsl>: |
            {
              if(this::class == KotlinJsTargetDsl::class)
                binaries.executable()
            }
