script:
  - plugins:
      - id: ("java-platform")
  - javaPlatform: |
      {
        allowDependencies()
      }
  - dependencies:
      constraints:
        - api: (project(":klib-ksp"))
        - api: (project(":klib"))
        - api: (project(":clib"))
        - api: (project(":shared"))
