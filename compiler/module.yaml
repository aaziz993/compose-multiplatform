product:
  type: lib
  platforms: [ jvm ]

apply:
  - ../template/common.module-template.yaml

dependencies:
  - ../kmp-lib
  - $libs.symbol.processing.api

settings:
  publishing:
    name: compiler
