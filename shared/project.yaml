templates:
  - ../template/common.yaml
  - ../template/kmp.yaml
  - ../template/cmp.yaml

plugins:
  ksp:
    processors:
      - :compiler

kotlin:
  sourceSets:
    - commonMain:
        dependencies:
          - :kmp-lib
          - :cmp-lib
