templates:
  - ../template/common.yaml
  - ../template/compose.yaml

application: true

kotlin:
  wasmJs:
    '': { }

  sourceSets:
    commonMain:
      dependencies:
        - ../kmp-lib
        - ../cmp-lib
        - ../shared
