application: true

targets: [ wasm ]

apply:
  - ../template/common.module-template.yaml
  - ../template/compose.module-template.yaml

dependencies:
  - ../kmp-lib
  - ../cmp-lib
  - ../shared

settings:
  web:
    executable: true
