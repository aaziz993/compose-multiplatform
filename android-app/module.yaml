product: android/app

apply:
  - ../template/common.module-template.yaml
  - ../template/compose.module-template.yaml
  - ../template/application.module-template.yaml
  - ../template/room.module-template.yaml

dependencies:
  - ../kmp-lib
  - ../cmp-lib
  - ../shared
  - $libs.androidx.activity.compose

settings:
  android:
    namespace: io.github.aaziz993.android.app
    minSdk: 35
    signing:
      enabled: true
      propertiesFile: ./keystore.properties # default
