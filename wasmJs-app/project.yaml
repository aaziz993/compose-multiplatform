templates:
  - ../template/common.yaml
  - ../template/compose.yaml

kotlin:
  wasmJs:
    '':
      browser: { }
      nodejs: { }
      executable: true

  sourceSets:
    commonMain:
      dependencies:
        - ../kmp-lib
        - ../cmp-lib
        - ../shared
    wasmJsMain:
      dependencies:
        - $kotlinWrappers.browser
