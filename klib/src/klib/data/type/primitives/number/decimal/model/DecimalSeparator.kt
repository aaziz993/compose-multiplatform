package klib.data.type.primitives.number.decimal.model

/**
 * Enum representing the decimal separator options.
 */
public enum class DecimalSeparator(public val char: Char) {
    COMMA(','),
    DOT('.')
}

