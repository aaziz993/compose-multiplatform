config:
  imports:
    - io.ktor.server.plugins.httpsredirect.HttpsRedirect
script:
  - module: |
      {
        install(HttpsRedirect) {
          sslPort = 8443
          permanentRedirect = true
        }
      }
