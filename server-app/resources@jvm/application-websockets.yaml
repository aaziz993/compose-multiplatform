config:
  imports:
    - io.ktor.server.websocket.WebSockets
script:
  - module: |
      {
        install(WebSockets) {
          pingPeriodMillis= 15000
          timeoutMillis = 15000
          maxFrameSize = Long.MAX_VALUE
          masking = false
        }
      }

#  page:
#    - enable: true
#      uri:
#      wsURI:
